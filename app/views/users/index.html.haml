%h3 All Users

%table
  %tr
    %th Name
    %th SshKeys
    %th Accounts
    %th Controls

  - @users.each do |user|
    %tr
      %td= user.email
      %td
        = user.ssh_keys.count
      %td
        = user.accounts.count
      %td
        = link_to 'View', user_path(user)
        = link_to 'Delete', user_path(user), :method => :delete, :confirm => 'Are you sure you want to remove this user?'
